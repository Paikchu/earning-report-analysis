/** Where `vinext build` writes the Web Worker's Wrangler config. */
export const WEB_WORKER_CONFIG_PATH = "dist/server/wrangler.json";

/** The Pipeline Worker's committed config — the analysis backend's source of truth. */
export const PIPELINE_WORKER_CONFIG_PATH = "workers/pipeline/wrangler.jsonc";
